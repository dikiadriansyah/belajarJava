public class xxallocclearchunksize {
    // -XXallocClearChunkSize

    /*
     * Saat digunakan dengan -XXallocClearChunkSize, opsi ini mengatur ukuran
     * potongan yang akan dihapus. Jika tanda ini digunakan tetapi tidak ada nilai
     * yang ditentukan, nilai defaultnya adalah 512 byte.
     */

    /*
     * gunakan:
     * -XXallocClearChunks -XXallocClearChunkSize=<size>[k|K][m|M][g|G]
     */
}
