public class Entering_and_existing_jshell {
    // Entering and Exiting JShell/Masuk dan Keluar JShell

    /*
     * Starting JShell
     * 
     * Sebelum mencoba memulai JShell, pastikan variabel lingkungan JAVA_HOME Anda
     * mengarah ke instalasi JDK 9. Untuk memulai JShell, jalankan perintah berikut:
     */

    // $ jshell

    // Jika semuanya berjalan dengan baik, Anda akan melihat prompt jshell>.

    // Exiting JShell

    // Untuk keluar dari JShell, jalankan perintah berikut dari prompt JShell:

    // jshell> /exit

}
